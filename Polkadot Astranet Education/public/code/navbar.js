// Minimal navbar placeholder for login page
export function initNavbar() {
  // Additional navbar logic could go here
}

if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', initNavbar);
} else {
  initNavbar();
}
